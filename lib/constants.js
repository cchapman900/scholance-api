// These apply to the OAuth scopes (permissions) for authorization

module.exports = Object.freeze({
    SCOPES: {
        MANAGE_ENTRY: 'manage:entry',
        MANAGE_PROJECT: 'manage:project'
    }
});